name: ledmatrix
description: "App to control LedMatrixOS"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flex_color_scheme: ^8.4.0
  flutter:
    sdk: flutter
  google_fonts: ^8.1.0
  http: ^1.6.0
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
